# Neon Postgres (console.neon.tech -> project lea -> Connect)
DATABASE_URL=

# Neon Auth base URL (console -> project -> branch -> Auth -> Configuration)
NEON_AUTH_BASE_URL=

# Generate with: openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET=
